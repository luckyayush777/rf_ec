{
  "name": "bench-gpu-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "model:build": "node scripts/blender.mjs build",
    "model:export": "node scripts/blender.mjs export"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "~5.9.2",
    "vite": "^7.1.7"
  }
}
